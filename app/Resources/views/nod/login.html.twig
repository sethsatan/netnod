{% if is_granted('ROLE_USER') %} 
   {{ render(controller('NodUserBundle:User:IsLogger')) }}             
{% else %}
    <ul class="nav top-nav">
    {{ render(controller('FOSUserBundle:Security:login')) }}
    </ul>
{% endif %}